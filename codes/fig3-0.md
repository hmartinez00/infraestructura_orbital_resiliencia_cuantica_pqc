Procedo a redactar el prompt de ingeniería de imágenes completamente optimizado para **`fig3.png`**.

Este prompt traduce la pila de cinco capas en un bloque jerárquico unificado que integra el bucle de control autónomo descendente (agilidad en tiempo real) y el circuito lógico en paralelo del handshake híbrido (convergencia de la ecuación 2), omitiendo por completo el texto incrustado para permitir una rotulación limpia en LaTeX mediante `overpic`.

### PROMPT DE IMAGEN OPTIMIZADO (Arquitectura en Capas y Flujo Criptográfico IEEE):

```json
A 2D technical vector diagram, architectural layer stack and cryptographic circuit schematic, flat design, minimalist, background in pure white, utilizing a strict color palette of cobalt blue #0047AB, technical grey #4A4A4A, and deep black for main vector lines. The image contains NO embedded text, numbers, or alphabetic labels.

The diagram is organized horizontally into three main interconnected sections.

Left Section (Orbital Constellation Context): Two concentric, thin circular orbit lines in technical grey #4A4A4A populate the upper-left space, containing multiple minimalist satellite vector icons linked together via fine dashed lines (representing inter-satellite links, ISL). A down-pointing vector connects from these orbits to a ground station antenna icon at the lower-left. A main horizontal data bus line connects this entire space to the central stack.

Center Section (The 5-Layer Cryptographic Stack): A rigid vertical alignment of five identical, neat rectangular blocks stacked on top of each other. From bottom to top, they are marked only on their left side with discrete Roman numerals (I, II, III, IV, V). 
- Block I (Bottom - Physical & Link): Contains a stylized wave vector intersecting a Doppler frequency shift graphic in technical grey #4A4A4A.
- Block II (Transport - Cobalt Blue #0047AB): Acts as the main integration anchor, extending a connection line to the right.
- Block III (Key Management - Cobalt Blue #0047AB): Inside, a minimalist 2D binary tree structure is drawn with clean thin white lines (representing tree-based key distribution).
- Block IV (Resilience Orchestration - Cobalt Blue #0047AB): Features a tiny gear and shield icon combined. From this block, a prominent curved, dashed cobalt blue control loop arrow loops outward and downward, injecting directly into the side of Block II and Block III (symbolizing autonomous real-time cryptographic agility).
- Block V (Top - Application): Features a clean API network node icon.

Right Section (Hybrid Handshake Circuit Breakdown): Connected directly via a parallel bus from Block II (Transport), a sub-schematic expands horizontally to map the KDF equation logic. Two horizontal parallel lines enter from the left: the top line carries a stylized classic elliptic curve symbol (representing ECC); the bottom line carries a 2D vector crystal lattice icon (representing ML-KEM). Both input lines converge into a central circular concatenation node containing a parallel symbol (||), which then flows into a single rectangular filter block (representing the Key Derivation Function, KDF). The output line exits this block with a solid, clean key icon, looping back securely into the main transport layer.

Technical details: Sharp geometric symmetry, crisp orthogonal lines (1.5pt for main blocks, 1pt for internal circuit paths), no shadows, no gradients, matching IEEE journal publication style perfectly. All labels are omitted for LaTeX overpic insertion.

```